# PowerSync WebSocket proxy — include this in the nginx HTTPS server block.
# Proxies wss://nginx/powersync/ → ws://powersync:8080/
# This avoids mixed content (ws:// from https:// page) and CSP issues.

location /powersync/ {
    proxy_pass http://powersync:8080/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;
}
